# Performance

Host numbers below are Windows Visual Studio 2026 Release. Tab5 numbers
are a physical ESP32-P4 run (`docs/evidence/TAB5_SPRINGFIELD_HARDWARE.md`).
They are comparative evidence, not interchangeable.

```
HOST PERFORMANCE — NOT ESP32 PERFORMANCE
```

```
ESP32-P4 / TAB5 HARDWARE — RESULT PASS
```

## What was measured (2026-09-14)

Pack: `examples/m5stack-tab5/test-pack/springfield-97477.pmtiles`
(3,507,636 bytes, PMTiles v3,
MVT, gzip tiles, z0–15, 141 addressed tiles). Regenerated by
`tools/pack-builder/build_springfield_pack.ps1`.

Viewport: 1280×720, `tile_size_px=256`, center 44.0500°N, 123.0220°W,
style `orcsdr-dark`. Tiles from `EnumerateVisibleTiles`.

The original **221 ms** z14 frame was mostly **printf of per-tile reports**,
not engine work. Re-instrumented (quiet one-line tiles, 2026-09-14, after
styled widths):

| Stage (20 present tiles) | HOST ms |
|---|---|
| `GetTile` | 0.61 |
| `DecompressPayload` | 7.05 |
| `DecodeMvtTile` | 26.74 |
| `TranslateMvtToFeatureTile` | 8.87 |
| `AssignFeatureKinds` | 0.44 |
| `RenderFeatureTile` | 4.90 |
| report/print | 0.03 |
| WritePpm | 17.89 |
| accounted | 66.52 |
| wall-clock frame | **74.60** |
| unaccounted (open/clear/alloc) | 8.08 |

Decode dominates. Thick-line rasterization added a few ms vs the old
~4.4 ms 1px render-sum; still small vs decode. HOST ONLY.

Per-tile HOST cost on a busy z14 downtown tile (`14/2593/5953`, 88,658
stored / 141,658 raw bytes, 2,496 features), from the earlier quiet-less
run:

| Stage | HOST ms |
|---|---|
| `GetTile` | 0.03 |
| `DecompressPayload` | 0.61 |
| `DecodeMvtTile` | 2.21 |
| `TranslateMvtToFeatureTile` | 0.71 |
| `AssignFeatureKinds` | 0.03 |
| `RenderFeatureTile` | ~0.2–0.25 |

## Tile payload sizes (MEASURED, this pack only)

z14 sample, 20 present tiles around the preview center:

| | stored (gzip) | decompressed |
|---|---|---|
| min | 14,092 | 19,623 |
| median | 64,662 | 99,833 |
| max | 88,658 | 141,658 |

z15 sample (9 tiles at radius 1): stored 18,185–33,272, decompressed
26,510–51,754.

**Provisional ESP32 caller budget (not a core constant):** 512 KiB per
tile covers this pack’s max (~139 KiB) with margin. 1 MiB is a
comfortable first SD-path budget. Do not freeze a global OrcMaps limit
from this one city extract.

## FeatureTile expansion (MEASURED, approximate)

Busy z14 tile `14/2593/5953`:

- 2,496 features, 5,814 paths, 23,776 vertices
- 6,049 property keys, ~113 KB of owned strings
- approximate FeatureTile owned bytes: ~1.2 MiB
- vs 142 KB decompressed MVT (~8×)

The preview tool streams one source tile: drop compressed bytes after
decompress, drop raw after MVT decode, drop `MvtTile` after translate,
render, then `FeatureTile::features.clear()`. Peak overlap during one
busy tile is approximately FeatureTile (~1.2 MiB) plus a shrinking
`MvtTile` during translate, plus render scratch. Do not keep a
viewport-wide FeatureTile cache.

The current `MvtTile` + `FeatureTile` deep copy is acceptable if the
engine keeps **one source tile** in RAM at a time (ESP32-P4 + PSRAM).
Holding a full 20-tile viewport of FeatureTiles would be ~20 MiB and
is not the intended embedded path. Layer-string duplication is real
(`layer` copied onto every feature) but not the first blocker.

## Reference numbers from prior art (not OrcMaps)

Cited in `docs/FORMAT_DECISION.md`:

- `yuiseki/m5-cardputer-offgrid-tiny-map` (ESP32-S3, no PSRAM): z13 tile
  render 33s → 8.1s after optimization, z0 0.4s.

## Tab5 hardware (ESP32-P4)

Same viewport as the host table. Sequential SD read ~1.73 MB/s at 40 MHz
4-bit. Full log: `docs/evidence/TAB5_SPRINGFIELD_HARDWARE.md`.

**BEFORE no-text layer filter** (firmware `1427597`): cold frame
**11302 ms**. Decode 5612 ms, translate 2693 ms, render 1246 ms, GetTile
610 ms, gzip 170 ms. Internal heap min ~40 KiB during MvtTile decode.

**AFTER no-text layer filter / CURRENT** (`b396c7d`): cold frame
**4421 ms**. Decode 1296 ms, translate 707 ms, classify 9 ms, render
1239 ms, GetTile ~610 ms, gzip ~170 ms.

## Tab5 after no-text layer filter (same viewport)

Skip housenumber / poi / place / transportation_name / water_name /
mountain_peak at decode (generic `include_layer` callback; names live in
experimental policy, not the MVT parser).

| Stage | Before ms | After ms | Δ |
|---|---|---|---|
| MVT decode | 5612 | **1296** | −77% |
| translate | 2693 | **707** | −74% |
| classify | 66 | 9 | |
| render | 1246 | 1239 | ~0 |
| **cold frame** | 11302 | **4421** | **−61%** |

Visible 24 / present 20 / missing 4 unchanged. First busy tile kept 8
layers / 202 features (was 12 layers / 2071). Internal heap after that
MvtTile: ~140 KiB vs ~40 KiB. Render time unchanged.

## Still unmeasured

Pan/zoom latency. Cache hit rates. Direct MVT→FeatureTile path.
